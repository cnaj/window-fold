{
  "name": "window-fold",
  "main": "./lib/window-fold",
  "version": "0.0.1",
  "description": "Keeps the text area at the same size when panels are opened or closed.",
  "keywords": [
  ],
  "activationCommands": {
    "atom-workspace": "window-fold:toggle"
  },
  "repository": "https://github.com/cnaj/window-fold",
  "license": "BSD-3-Clause",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
  }
}
